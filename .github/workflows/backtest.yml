name: SuperTrend v8 Backtest

on:
  workflow_dispatch: {}

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run backtest
        run: python backtest.py

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: backtest-v8-results
          path: |
            backtest_summary.txt
            backtest_report.json
          if-no-files-found: warn
